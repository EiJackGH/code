## 2024-05-22 - CLI Colorization
**Learning:** Adding simple ANSI colors and emojis to a Python CLI script significantly improves readability without needing external libraries like `rich` or `colorama`.
**Action:** For simple scripts, use a lightweight `Colors` class with ANSI codes instead of heavy dependencies to add delight.
